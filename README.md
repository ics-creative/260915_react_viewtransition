# React View Transition Demo

Run `npm ci`, then `npm run dev`. Use `npm run build` to type-check and build, `npm run lint` to lint, and `npm run format` to format.

Reference: [ICS MEDIA](https://ics.media/entry/230510/). Photos by IKEDA Yasunobu.
